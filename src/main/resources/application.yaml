spring:
    datasource:
        url: jdbc:mysql://localhost:3306/infuse
        username: root
        password: root
    jpa:
        show-sql: true
        properties:
            hibernate:
                format_sql: true
server:
    error:
        include-stacktrace: never